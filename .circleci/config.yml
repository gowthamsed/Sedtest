version: 2.1

orbs:
  sedstart: sedstart/cicd@dev:1

jobs:
   run-sedstart-local-windows:
    resource_class: 'windows.medium'
    machine:
      image: "windows-server-2022-gui:current"
      shell: "powershell.exe -ExecutionPolicy Bypass"
    steps:
      - checkout
      - sedstart/runlocal:
          api_key: SEDSTART_API_KEY
          project_id: 422
          test_id: 3490
          profile_id: 609
          browser: "chrome"

workflows:
  test-workflow:
    jobs:
      - run-sedstart-local-windows
